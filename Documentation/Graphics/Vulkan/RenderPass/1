std::vector<VkSubpassDependency> dependencies(3);

// 依赖 0: Subpass 0 → Subpass 1 (计算写 → 计算读)
dependencies[0].srcSubpass = 0;
dependencies[0].dstSubpass = 1;
dependencies[0].srcStageMask = VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT;
dependencies[0].dstStageMask = VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT;
dependencies[0].srcAccessMask = VK_ACCESS_SHADER_WRITE_BIT;      // 计算写入
dependencies[0].dstAccessMask = VK_ACCESS_SHADER_READ_BIT;       // 计算读取
dependencies[0].dependencyFlags = VK_DEPENDENCY_BY_REGION_BIT;

// 依赖 1: Subpass 1 → Subpass 2 (计算写 → 图形读)
dependencies[1].srcSubpass = 1;
dependencies[1].dstSubpass = 2;
dependencies[1].srcStageMask = VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT;
dependencies[1].dstStageMask = VK_PIPELINE_STAGE_FRAGMENT_SHADER_BIT;
dependencies[1].srcAccessMask = VK_ACCESS_SHADER_WRITE_BIT;
dependencies[1].dstAccessMask = VK_ACCESS_INPUT_ATTACHMENT_READ_BIT;
dependencies[1].dependencyFlags = VK_DEPENDENCY_BY_REGION_BIT;

// 依赖 2: 外部 → Subpass 0 (可选，用于布局转换)
dependencies[2].srcSubpass = VK_SUBPASS_EXTERNAL;
dependencies[2].dstSubpass = 0;
dependencies[2].srcStageMask = VK_PIPELINE_STAGE_TOP_OF_PIPE_BIT;
dependencies[2].dstStageMask = VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT;
dependencies[2].srcAccessMask = 0;
dependencies[2].dstAccessMask = VK_ACCESS_SHADER_WRITE_BIT;