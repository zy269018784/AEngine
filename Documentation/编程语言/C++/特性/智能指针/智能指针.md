| 智能指针 | 引入版本 | 头文件 | 所有权模型 | 引用计数 | 支持数组 | 可拷贝 | 可移动 | 核心用途 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `std::auto_ptr` | C++98 (已废弃) | `<memory>` | 独占（转移语义） | ❌ | ❌ | ❌ (拷贝即转移) | ✅ | 已被 unique_ptr 替代 |
| `std::unique_ptr` | C++11 | `<memory>` | 独占（唯一所有权） | ❌ | ✅ (C++17) | ❌ | ✅ | 独占资源，轻量高效 |
| `std::shared_ptr` | C++11 | `<memory>` | 共享（多个所有者） | ✅ (线程安全) | ✅ (C++17) | ✅ | ✅ | 多个对象共享同一资源 |
| `std::weak_ptr` | C++11 | `<memory>` | 弱引用（不参与所有权） | ❌ (观察 shared_ptr) | ❌ | ✅ | ✅ | 打破循环引用，观察者模式 |




```cpp
#include <iostream>
#include <memory>

class Test {
public:
    int id;
    Test(int i) : id(i) { std::cout << "构造 " << id << "\n"; }
    ~Test() { std::cout << "析构 " << id << "\n"; }
    void hello() { std::cout << "Hello " << id << "\n"; }
};

int TestCXX(int argc, char **argv) 
{
    // 1. 创建 unique_ptr
    auto p1 = std::make_unique<Test>(1);
    
    // 2. 访问成员
    p1->hello();           // 方式1
    (*p1).hello();         // 方式2
    
    // 3. 获取裸指针
    Test* raw = p1.get();
    std::cout << "裸指针地址: " << raw << "\n";
    
    // 4. 转移所有权（移动）
    std::unique_ptr<Test> p2 = std::move(p1);
    if (!p1) std::cout << "p1 已空\n";
    p2->hello();
    
    // 5. 释放所有权（不删除对象）
    Test* raw2 = p2.release();
    std::cout << "release 后 p2 为空\n";
    delete raw2;  // 必须手动删除
    
    // 6. 重置
    auto p3 = std::make_unique<Test>(3);
    p3.reset(new Test(33));  // 先删除原对象，再接管新对象
    p3.reset();               // 删除对象，变为空
    
    return 0;
}
```





```cpp
#include <iostream>
#include <memory>

class Test {
public:
    int id;
    Test(int i) : id(i) { std::cout << "构造 " << id << "\n"; }
    ~Test() { std::cout << "析构 " << id << "\n"; }
    void hello() { std::cout << "Hello " << id << "\n"; }
};

int TestCXX(int argc, char **argv) 
{
    // 1. 创建 shared_ptr
    auto p1 = std::make_shared<Test>(1);
    
    // 2. 访问成员
    p1->hello();
    (*p1).hello();
    
    // 3. 查看引用计数
    std::cout << "引用计数: " << p1.use_count() << "\n";
    
    // 4. 拷贝（共享所有权）
    auto p2 = p1;  // 引用计数 +1
    std::cout << "拷贝后引用计数: " << p1.use_count() << "\n";
    
    // 5. 获取裸指针
    Test* raw = p1.get();
    std::cout << "裸指针地址: " << raw << "\n";
    
    // 6. 重置
    p2.reset();  // p2 释放，引用计数 -1
    std::cout << "p2 reset 后引用计数: " << p1.use_count() << "\n";
    
    // 7. 使用别名构造（共享同一个控制块，但指向不同成员）
    auto p3 = std::make_shared<Test>(2);
    std::shared_ptr<int> p4(p3, &p3->id);  // 指向 id 成员
    std::cout << "p3 引用计数: " << p3.use_count() << "\n";
    
    return 0;
}
```



















