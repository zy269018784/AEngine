| 接口 | 引入版本 | 头文件 | 类型 | 核心用途 |
| :--- | :--- | :--- | :--- | :--- |
| **线程管理** | | | | |
| `std::thread` | C++11 | `<thread>` | 底层 | 创建和管理线程 |
| `std::jthread` | C++20 | `<thread>` | 中层 | 自动 join 的可中断线程 |
| **异步结果传递** | | | | |
| `std::promise` | C++11 | `<future>` | 中层 | 异步结果的**写入端**（一次性） |
| `std::future` | C++11 | `<future>` | 中层 | 异步结果的**读取端**（一次性） |
| `std::shared_future` | C++11 | `<future>` | 中层 | 异步结果的**读取端**（可多次/多线程） |
| `std::packaged_task` | C++11 | `<future>` | 中层 | 将可调用对象包装为异步任务 |
| `std::async` | C++11 | `<future>` | 高层 | 最简单的异步任务启动 |
| **互斥锁** | | | | |
| `std::mutex` | C++11 | `<mutex>` | 同步 | 互斥锁，保护共享数据 |
| `std::recursive_mutex` | C++11 | `<mutex>` | 同步 | 允许同一线程重复加锁 |
| `std::timed_mutex` | C++11 | `<mutex>` | 同步 | 带超时的互斥锁 |
| `std::recursive_timed_mutex` | C++11 | `<mutex>` | 同步 | 可递归 + 超时 |
| `std::shared_mutex` | C++17 | `<shared_mutex>` | 同步 | 读写锁（共享/独占） |
| `std::shared_timed_mutex` | C++14 | `<shared_mutex>` | 同步 | 带超时的读写锁 |
| **锁管理（RAII）** | | | | |
| `std::lock_guard` | C++11 | `<mutex>` | 同步 | 简单的 RAII 锁包装 |
| `std::unique_lock` | C++11 | `<mutex>` | 同步 | 灵活的 RAII 锁包装（支持延迟/超时/转移） |
| `std::shared_lock` | C++14 | `<shared_mutex>` | 同步 | RAII 读锁包装（用于 shared_mutex） |
| `std::scoped_lock` | C++17 | `<mutex>` | 同步 | 可同时锁定多个互斥量（避免死锁） |
| **条件变量** | | | | |
| `std::condition_variable` | C++11 | `<condition_variable>` | 同步 | 等待条件变化（与 unique_lock 配合） |
| `std::condition_variable_any` | C++11 | `<condition_variable>` | 同步 | 任意锁类型的条件变量 |
| **信号量** | | | | |
| `std::counting_semaphore` | C++20 | `<semaphore>` | 同步 | 计数信号量 |
| `std::binary_semaphore` | C++20 | `<semaphore>` | 同步 | 二元信号量（计数为 1 的特化） |
| **屏障** | | | | |
| `std::latch` | C++20 | `<latch>` | 同步 | 一次性计数器屏障（倒计数） |
| `std::barrier` | C++20 | `<barrier>` | 同步 | 可复用的阶段屏障 |
| **原子操作** | | | | |
| `std::atomic` | C++11 | `<atomic>` | 同步 | 原子类型模板 |
| `std::atomic_flag` | C++11 | `<atomic>` | 同步 | 原子布尔标志（锁自由实现） |
| `std::atomic_ref` | C++20 | `<atomic>` | 同步 | 对非原子对象的原子引用 |
| **工具函数** | | | | |
| `std::this_thread` 命名空间 | C++11 | `<thread>` | 工具 | 当前线程工具（sleep/yield/get_id） |
| `std::stop_token` / `std::stop_source` | C++20 | `<stop_token>` | 工具 | 线程协作中断机制 |
| `std::call_once` / `std::once_flag` | C++11 | `<mutex>` | 同步 | 一次性初始化 |
| `std::notify_all_at_thread_exit` | C++11 | `<condition_variable>` | 同步 | 线程退出时通知 |
# std::promise 和 std::future 示例

```cpp
#include <iostream>
#include <future>
#include <thread>

int TestCXX(int argc, char **argv) 
{
    // 1. 创建 promise 和 future
    std::promise<int> promise;
    std::future<int> future = promise.get_future();

    // 2. 在另一个线程中设置值
    std::thread t([&promise]() {
        std::this_thread::sleep_for(std::chrono::seconds(1));
        promise.set_value(42);  // 设置值，唤醒等待的线程
    });

    // 3. 在主线程中等待并获取值
    std::cout << "Waiting for value..." << std::endl;
    int result = future.get();  // 阻塞直到 promise.set_value()
    std::cout << "Got: " << result << std::endl;

    t.join();
    return 0;
}