这板子呢，上面接有步进电机，然后485上面接的是那个激光传感器，IO口呢，
输入和输出，输入接个脚踏板，输出到时候接到那个。他那个原来那个脚踏板书接到那个激光打标机上去。

然后，底下下位机呢，其实就是读数据，读激光，不是读激光，读激光测距传感器的距离的数据。
然后根据上位机的判断，判断完之后，然后告诉。下位机步进电机要走多少距离，然后下位机就3个单片机当中去控制步进电机的驱动器就可以了。


要控制的模块:
    485     ---- 激光传感器
    GPIO    ---- 脚踏板
                 步进电机



工作:
    移植HAL
    移植RThread
    移植modbus RTU
    pwm   电机
    gpio  脚踏板
    modbus  激光





